$Id$
Ugh, how does this work...

run make check
Don't forget to add the files to the examples directory too (and its M.am)

* test-ycpdoc
Tests parsing (XML).
For each FOO.out (expected output), parses FOO*.ycp, uses FOO.out.tmp
for the actual output.

* test-html
Tests HTML output.
For each FOO.drv (dummy file), or only for $1, ...
